(()=>{const ctx=document.modelContext;if(!ctx?.registerTool)return;const controller=new AbortController();const tools=[{name:'read_puzzle',description:'Read the current visible puzzle, object attributes, arrangement and discovered rules without revealing hidden solutions.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:()=>({chapter:current+1,title:levels[current].title,objects:order.map((id,slot)=>({id,slot:slot+1,label:describe(id)})),found:found[current].map((v,i)=>v?levels[current].names[i]:null)})},{name:'swap_puzzle_objects',description:'Swap two occupied positions in the current puzzle, updating the canvas and evaluating discovered patterns.',inputSchema:{type:'object',properties:{positions:{type:'array',items:{type:'integer',minimum:1,maximum:9},minItems:2,maxItems:2}},required:['positions'],additionalProperties:false},annotations:{readOnlyHint:false},execute:input=>{const p=input?.positions;if(!Array.isArray(p)||p.length!==2||p.some(v=>!Number.isInteger(v)||v<1||v>order.length)||p[0]===p[1])throw Error('Choose two distinct occupied positions.');exchange(p[0]-1,p[1]-1);selected=null;render();return{order:[...order],moves:moveCount,found:[...found[current]]}}}];for(const tool of tools){try{Promise.resolve(ctx.registerTool(tool,{signal:controller.signal})).catch(()=>{})}catch{}}window.addEventListener('pagehide',()=>controller.abort(),{once:true})})();
