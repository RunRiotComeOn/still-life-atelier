(()=>{const ctx=document.modelContext;if(!ctx?.registerTool)return;const controller=new AbortController();const tools=[{name:'set_game_language',description:'Change the game language immediately while preserving the puzzle, discoveries, history, and painting side. Saves the preference on this device.',inputSchema:{type:'object',properties:{language:{type:'string',enum:['en','zh','ja','es','fr','de']}},required:['language'],additionalProperties:false},annotations:{readOnlyHint:false},execute:input=>{setLanguage(input?.language);return{language,title:levels[current].title,chapter:current+1,order:[...order]}}},{name:'open_puzzle',description:'Open one of the five chapters, starting a fresh arrangement and preserving discoveries.',inputSchema:{type:'object',properties:{chapter:{type:'integer',minimum:1,maximum:5}},required:['chapter'],additionalProperties:false},annotations:{readOnlyHint:false},execute:input=>{if(!Number.isInteger(input?.chapter)||input.chapter<1||input.chapter>5)throw Error('Choose chapter 1 through 5.');load(input.chapter-1);return{chapter:current+1,order:[...order]}}},{name:'flip_painting',description:'Flip the current painting puzzle to inspect its other side without moving pieces. Only available in chapter four.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:false},execute:()=>{if(current!==3)throw Error('Only chapter four has a reverse side.');flipCanvas();return{face:back?'back':'front',order:[...order]}}},{name:'read_puzzle',description:'Read the current visible puzzle, object attributes, arrangement and discovered rules without revealing hidden solutions.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:()=>({chapter:current+1,language,title:levels[current].title,face:current===3?(back?"back":"front"):null,objects:order.map((id,slot)=>({id,slot:slot+1,label:describe(id)})),found:found[current].map((v,i)=>v?levels[current].names[i]:null)})},{name:'swap_puzzle_objects',description:'Swap two occupied positions in the current puzzle, updating the canvas and evaluating discovered patterns.',inputSchema:{type:'object',properties:{positions:{type:'array',items:{type:'integer',minimum:1,maximum:9},minItems:2,maxItems:2}},required:['positions'],additionalProperties:false},annotations:{readOnlyHint:false},execute:input=>{const p=input?.positions;if(!Array.isArray(p)||p.length!==2||p.some(v=>!Number.isInteger(v)||v<1||v>order.length)||p[0]===p[1])throw Error('Choose two distinct occupied positions.');exchange(p[0]-1,p[1]-1);selected=null;render();return{order:[...order],moves:moveCount,found:[...found[current]]}}}];for(const tool of tools){try{Promise.resolve(ctx.registerTool(tool,{signal:controller.signal})).catch(()=>{})}catch{}}window.addEventListener('pagehide',()=>controller.abort(),{once:true})})();


